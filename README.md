A simple slides for `html` & `css`, focus on the basic concept
+ what is html
+ what is css
+ normal flow, float, position 

`lightbox`folder is the model of **Lightbox** showed on session

`index.html`is the slide